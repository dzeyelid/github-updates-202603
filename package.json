{
  "name": "github-updates-202603-slides",
  "version": "1.0.0",
  "description": "GitHub Copilot & GitHub Updates 2026/03 - Marp Slides",
  "scripts": {
    "build": "marp slides/presentation.md --output output/index.html --html && mkdir -p output/images && cp -r slides/images/. output/images/",
    "build:pdf": "marp slides/presentation.md --output output/presentation.pdf --pdf --html --allow-local-files",
    "preview": "marp slides/presentation.md --preview --html"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.0.0"
  }
}
